[project]
name = "poc-eight-gateway"
version = "0.1.0"
description = "POC Eight: Azure Service Bus Gateway"
requires-python = ">=3.12"
dependencies = [
  "azure-servicebus>=7.12.0",
  "python-dotenv>=1.0.0",
]

[tool.uv]
required-version = ">=0.9.7"
package = false
